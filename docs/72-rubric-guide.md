# Rubric Guide

The assignment platform reuses the frozen rubrics, rubric_versions and rubric_criteria runtime model. Criteria carry weighted scoring policy and versioned performance levels. Weights must total 100 before publication.

Published rubric versions are immutable and are pinned into assignment and submission versions. Criterion scoring produces current draft rows plus append-only score events.
